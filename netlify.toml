[build]
  publish = "dist"
  command = "pnpm build"

[functions]
  [functions.node_bundle]
    external_node_modules = []

# Tu Edge Function exclusiva para OAuth
[[edge_functions]]
  path = "/api/auth/callback"
  function = "auth"