[build]
  publish = "dist"
  command = "pnpm build"

# Sintaxis correcta y moderna para activar esbuild en Netlify Functions
[functions]
  node_bundle = true

# Tu Edge Function exclusiva para el inicio de sesión OAuth
[[edge_functions]]
  path = "/api/auth/callback"
  function = "auth"