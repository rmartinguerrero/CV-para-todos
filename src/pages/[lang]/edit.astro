---
// src/pages/[lang]/edit.astro
import { useTranslations, type Lang, type UIKeys } from '../../i18n/ui';
import resumeEs from '../../data/resume.es.json';
import resumeIt from '../../data/resume.it.json';
import resumeEn from '../../data/resume.en.json';
import { CV_THEMES_CSS } from '../../styles/cv-themes';
import MinimalistLayout from '../../layouts/MinimalistLayout.astro';
import Footer from '../../components/Footer.astro';
  
export function getStaticPaths() {
  return [
    { params: { lang: 'es' } },
    { params: { lang: 'it' } },
    { params: { lang: 'en' } },
  ];
}

const { lang } = Astro.params as { lang: Lang };
const resumes = { es: resumeEs, it: resumeIt, en: resumeEn };
const initialData = resumes[lang];
const strictT = useTranslations(lang);
const t = (key: string) => strictT(key as UIKeys);

const layoutModules = import.meta.glob('../../layouts/*.astro', { eager: true });

const templates = Object.entries(layoutModules)
  .map(([path, mod]: [string, any]) => {
    if (!mod.templateMeta) return null;
    return {
      id: mod.templateMeta.id,
      displayName: mod.templateMeta.name,
      svg: mod.templateMeta.svg
    };
  })
  .filter(Boolean) as { id: string; displayName: string; svg: string }[];
---

<MinimalistLayout
  title={t('edit.title')}
  description={lang === 'es' ? 'Editor de currículum profesional sin código' : lang === 'it' ? 'Editor di curriculum professionale senza codice' : 'Professional no-code resume editor'}
  lang={lang}
  currentLang={lang}
  availableLangs={['es', 'it', 'en']}
  resume={initialData}
  t={t}
  robots="noindex, nofollow"
>

<a href="#cv-form" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:bg-blue-600 focus:text-white focus:px-4 focus:py-2 focus:rounded-lg focus:shadow-lg focus:outline-none focus:ring-2 focus:ring-blue-300">
  {t('nav.previousSection')}
</a>

<style is:global>
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }
  main { max-width: 100% !important; padding: 0 !important; margin: 0 !important; }
  .tab-btn:focus-visible,
  #cv-form button:focus-visible,
  #cv-form input:focus-visible,
  #cv-form textarea:focus-visible,
  #cv-form select:focus-visible {
    outline: 2px solid #2563eb;
    outline-offset: 2px;
    border-radius: 4px;
  }
  body.minimalist-layout { background-color: #f9fafb !important; }
  #preview-section, #cv-form { overflow-wrap: anywhere; }
  #preview-section img, #cv-form img { overflow-wrap: normal; }
  .skill-tag-item { max-width: 100%; }
</style>

<!-- Google Fonts for theme layouts -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap" rel="stylesheet" />

<!-- Cookie Consent + Google Analytics (RGPD) -->
<script is:inline>
  (() => {
    const consent = localStorage.getItem('cookie-consent');
    if (consent !== 'accepted') return;
    window.dataLayer = window.dataLayer || [];
    function gtag(){ dataLayer.push(arguments); }
    window.gtag = gtag;
    gtag('js', new Date());
    gtag('config', 'G-R3TG90C8J5', { anonymize_ip: true });
    const gaScript = document.createElement('script');
    gaScript.async = true;
    gaScript.src = "https://www.googletagmanager.com/gtag/js?id=G-R3TG90C8J5";
    const firstScript = document.getElementsByTagName('script')[0];
    firstScript.parentNode.insertBefore(gaScript, firstScript);
  })();
</script>

<div class="bg-gray-50 min-h-screen text-gray-800 antialiased">

  <div class="w-full bg-white border-b border-gray-200 sticky top-0 z-50 print:hidden shadow-sm">
    <div class="hidden lg:flex items-center justify-between gap-4 py-2.5 px-4 sm:px-6">
      <a href={`/${lang}`} class="flex items-center gap-2">
        <div class="bg-blue-600 text-white p-2 rounded-lg shadow-sm">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
        </div>
        <div class="flex flex-col items-start">
          <span class="text-sm font-black tracking-tight text-gray-900 leading-none">{t('brand.name')}</span>
        </div>
      </a>

      <div class="inline-flex rounded-xl bg-gray-100 p-0.5 border border-gray-200 shadow-inner">
        <button onclick="window.location.href='/es/edit'" aria-label="Español" class={`px-2 py-1 rounded-lg text-sm leading-none transition-all ${lang === 'es' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>&#127466;&#127480;</button>
        <button onclick="window.location.href='/it/edit'" aria-label="Italiano" class={`px-2 py-1 rounded-lg text-sm leading-none transition-all ${lang === 'it' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>&#127470;&#127481;</button>
        <button onclick="window.location.href='/en/edit'" aria-label="English" class={`px-2 py-1 rounded-lg text-sm leading-none transition-all ${lang === 'en' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>&#127482;&#127480;</button>
      </div>

      <div class="flex items-center gap-2">
        <button id="btn-download-json" class="btn-download-json-control px-3 py-1.5 text-xs font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg border border-gray-200">{t('edit.downloadJson')}</button>
        <button id="btn-print" class="btn-print-control px-3 py-1.5 text-xs font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg border border-gray-200">{t('edit.downloadPdf')}</button>
        <button id="btn-github-auth" class="btn-github-auth-control px-3 py-1.5 text-xs font-semibold text-white bg-gray-800 hover:bg-gray-900 rounded-lg border border-gray-700 transition-all flex items-center gap-1.5">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" fill="currentColor" viewBox="0 0 16 16">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"/>
          </svg>
          <span id="github-status-text" class="github-status-text">GitHub</span>
        </button>
      </div>
    </div>

    <div class="lg:hidden">
      <div class="flex flex-wrap items-center justify-between gap-3 py-2.5 px-4 sm:px-6">
        <a href={`/${lang}`} class="flex items-center gap-2 flex-1 min-w-0">
          <div class="bg-blue-600 text-white p-1.5 rounded-lg items-center justify-center shadow-sm inline-flex">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          </div>
          <span class="text-sm font-semibold text-gray-900 truncate">{t('brand.name')}</span>
        </a>
        <div class="inline-flex rounded-lg bg-gray-100 p-0.5 border border-gray-200 shadow-inner">
          <button onclick="window.location.href='/es/edit'" aria-label="Español"
            class={`px-1.5 py-1 rounded-md text-xs leading-none transition-all ${lang === 'es' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>
            &#127466;&#127480;
          </button>
          <button onclick="window.location.href='/it/edit'" aria-label="Italiano"
            class={`px-1.5 py-1 rounded-md text-xs leading-none transition-all ${lang === 'it' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>
            &#127470;&#127481;
          </button>
          <button onclick="window.location.href='/en/edit'" aria-label="English"
            class={`px-1.5 py-1 rounded-md text-xs leading-none transition-all ${lang === 'en' ? 'bg-white text-gray-900 shadow-xs border border-gray-200/60' : 'text-gray-500 hover:text-gray-900'}`}>
            &#127482;&#127480;
          </button>
        </div>
        <button class="btn-print-control px-3 py-2 text-xs font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg border border-gray-200">{t('edit.downloadPdf')}</button>
      </div>
      <div class="flex flex-wrap items-center justify-between gap-3 border-t border-gray-100 py-2.5 px-4 sm:px-6">
        <button class="btn-download-json-control w-full sm:w-auto px-3 py-2 text-xs font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg border border-gray-200">{t('edit.downloadJson')}</button>
        <button class="btn-github-auth-control w-full sm:w-auto px-3 py-2 text-xs font-semibold text-white bg-gray-800 hover:bg-gray-900 rounded-lg border border-gray-700 transition-all flex items-center justify-center gap-1.5">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" fill="currentColor" viewBox="0 0 16 16">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"/>
          </svg>
          <span class="github-status-text">GitHub</span>
        </button>
      </div>
    </div>
  </div>

  <div class="flex flex-col lg:grid lg:grid-cols-2 gap-6 lg:gap-10 lg:items-start min-h-[calc(100vh-4rem)] p-4 sm:p-6 print:block print:p-0">

    <div class="bg-white rounded-2xl border border-gray-200 shadow-sm print:hidden flex flex-col lg:sticky lg:top-20 lg:max-h-[82vh] w-full min-w-0">

      <div class="px-4 sm:px-6 pt-4 sm:pt-6 pb-2 flex items-center justify-between border-b border-gray-100">
        <h1 class="text-base sm:text-lg font-bold text-gray-900 tracking-tight">{t('edit.title')}</h1>
      </div>

      <div class="px-4 sm:px-6 pt-3 relative">
        <button id="tab-prev" type="button" class="lg:hidden absolute left-0 top-1/2 z-10 -translate-y-1/2 rounded-full border border-gray-200 bg-white/90 px-1.5 py-1 text-sm text-gray-600 shadow-sm transition-all hover:bg-gray-100" aria-label={t('nav.previousSection')}>&lsaquo;</button>
        <nav id="tab-nav" class="flex text-xs sm:text-sm overflow-x-auto whitespace-nowrap scroll-smooth flex-nowrap touch-pan-x gap-0.5 sm:gap-1 px-5" role="tablist" aria-label={t('nav.tabs')}>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-blue-600 font-medium text-blue-600 flex-shrink-0" data-tab="basics" id="tab-btn-basics" role="tab" aria-selected="true" aria-controls="tab-basics">{t('nav.contact')}</button>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-transparent font-medium text-gray-500 hover:text-gray-700 flex-shrink-0" data-tab="work" id="tab-btn-work" role="tab" aria-selected="false" aria-controls="tab-work">{t('section.experience')}</button>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-transparent font-medium text-gray-500 hover:text-gray-700 flex-shrink-0" data-tab="education" id="tab-btn-education" role="tab" aria-selected="false" aria-controls="tab-education">{t('section.education')}</button>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-transparent font-medium text-gray-500 hover:text-gray-700 flex-shrink-0" data-tab="extras" id="tab-btn-extras" role="tab" aria-selected="false" aria-controls="tab-extras">{t('section.educationExtra')}</button>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-transparent font-medium text-gray-500 hover:text-gray-700 flex-shrink-0" data-tab="skills" id="tab-btn-skills" role="tab" aria-selected="false" aria-controls="tab-skills">{t('section.skills')}</button>
          <button class="tab-btn px-2.5 sm:px-4 py-2 border-b-2 border-transparent font-medium text-gray-500 hover:text-gray-700 flex-shrink-0" data-tab="templates" id="tab-btn-templates" role="tab" aria-selected="false" aria-controls="tab-templates">{t('templates.title')}</button>
        </nav>
        <button id="tab-next" type="button" class="lg:hidden absolute right-4 top-1/2 z-10 -translate-y-1/2 rounded-full border border-gray-200 bg-white/90 px-2 py-1 text-sm text-gray-600 shadow-sm transition-all hover:bg-gray-100" aria-label={t('nav.nextSection')}>&rsaquo;</button>
      </div>

      <div id="cv-form" class="flex-1 lg:overflow-y-auto px-4 sm:px-6 pt-3 pb-6 space-y-7">
        <div id="tab-basics" class="tab-content space-y-5" role="tabpanel" aria-labelledby="tab-btn-basics">
          <div class="grid sm:grid-cols-2 gap-4">
            <div>
              <label for="field-name" class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.name')}</label>
              <input type="text" id="field-name" data-path="basics.name" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
            </div>
            <div>
              <label for="field-label" class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.label')}</label>
              <input type="text" id="field-label" data-path="basics.label" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
            </div>
          </div>
          <div class="grid sm:grid-cols-2 gap-4">
            <div>
              <label for="field-email" class="block text-xs font-semibold text-gray-500 uppercase mb-1">Email</label>
              <input type="email" id="field-email" data-path="basics.email" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
            </div>
            <div>
              <label for="field-phone" class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.phone')}</label>
              <input type="tel" id="field-phone" data-path="basics.phone" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
            </div>
          </div>
          <div class="mt-3">
            <label for="field-web" class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.website')}</label>
            <input type="url" id="field-web" data-path="basics.web" placeholder="https://" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
          </div>
          <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.photo')}</label>
            <div class="flex flex-wrap items-center gap-2 sm:gap-3 bg-gray-50 p-3 rounded-xl border border-dashed border-gray-300">
              <input type="file" id="photo-file" accept="image/*" class="hidden" />
              <button type="button" onclick="document.getElementById('photo-file').click()" class="px-3 py-1.5 bg-white border border-gray-200 text-gray-700 text-xs font-medium rounded-lg hover:bg-gray-50 shadow-sm">
                {t('form.chooseFile')}
              </button>
              <button type="button" id="btn-remove-photo" class="text-xs text-red-500 font-medium hidden hover:underline">{t('form.remove')}</button>
              <span id="photo-status" class="text-xs text-gray-500 truncate max-w-[100px] sm:max-w-[180px]">{t('form.noFile')}</span>
            </div>
            <input type="url" id="photo-url" placeholder="https://" class="mt-2 w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500" />
            <p class="mt-1 text-xs text-gray-500">{t('form.photoHelp')}</p>
          </div>
          <div>
            <label for="field-summary" class="block text-xs font-semibold text-gray-500 uppercase mb-1">{t('form.summary')}</label>
            <textarea id="field-summary" data-path="basics.summary" rows="4" class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-blue-500"></textarea>
          </div>
        </div>

        <div id="tab-work" class="tab-content space-y-4 hidden" role="tabpanel" aria-labelledby="tab-btn-work">
          <div id="work-container" class="space-y-4"></div>
          <button type="button" id="btn-add-work" class="w-full py-2 bg-blue-50 hover:bg-blue-100 text-blue-600 text-xs font-semibold rounded-xl border border-dashed border-blue-200">
            {t('section.addExperience')}
          </button>
        </div>

        <div id="tab-education" class="tab-content space-y-4 hidden" role="tabpanel" aria-labelledby="tab-btn-education">
          <div id="education-container" class="space-y-4"></div>
          <button type="button" id="btn-add-edu" class="w-full py-2 bg-blue-50 hover:bg-blue-100 text-blue-600 text-xs font-semibold rounded-xl border border-dashed border-blue-200">
            {t('section.addEducation')}
          </button>
        </div>

        <div id="tab-extras" class="tab-content space-y-6 hidden" role="tabpanel" aria-labelledby="tab-btn-extras">
          <div>
            <div class="flex items-center justify-between border-b border-gray-100 pb-3 mb-3">
              <h2 class="text-sm font-bold text-gray-900">{t('section.certificatesTitle')}</h2>
            </div>
            <div id="certificates-container" class="space-y-4"></div>
            <button type="button" id="btn-add-certificate" class="w-full py-2 bg-blue-50 hover:bg-blue-100 text-blue-600 text-xs font-semibold rounded-xl border border-dashed border-blue-200">
              {t('section.addCertificate')}
            </button>
          </div>
          <div>
            <div class="flex items-center justify-between border-b border-gray-100 pb-3 mb-3">
              <h2 class="text-sm font-bold text-gray-900">{t('section.languages')}</h2>
            </div>
            <div id="languages-container" class="space-y-4"></div>
            <button type="button" id="btn-add-language" class="w-full py-2 bg-blue-50 hover:bg-blue-100 text-blue-600 text-xs font-semibold rounded-xl border border-dashed border-blue-200">
              {t('section.addLanguage')}
            </button>
          </div>
        </div>

        <div id="tab-skills" class="tab-content space-y-4 hidden" role="tabpanel" aria-labelledby="tab-btn-skills">
          <div class="hidden sm:block bg-blue-50/60 p-3.5 rounded-xl border border-blue-100 text-xs text-blue-800">
            <strong>{lang === 'es' ? 'Instrucciones:' : lang === 'it' ? 'Istruzioni:' : 'Instructions:'}</strong> {t('form.skillsHint')}
          </div>
          <div>
            <input type="text" id="skills-input" placeholder={t('form.skillsPlaceholder')} class="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm outline-none" />
          </div>
          <div id="skills-tag-container" class="flex flex-wrap gap-1.5 p-3 border border-gray-200 rounded-xl bg-gray-50/50 min-h-[60px]"></div>
        </div>

        <div id="tab-templates" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-btn-templates">
          <label class="block text-xs font-semibold text-gray-500 uppercase mb-3">
            {t('templates.subtitle') || (lang === 'es' ? 'Elige el aspecto visual de tu CV' : lang === 'it' ? "Scegli l'aspetto visivo del tuo CV" : 'Choose the visual style of your resume')}
          </label>
          <div class="grid grid-cols-2 sm:grid-cols-3 gap-4">
            {templates.map((tpl, i) => {
              const key = `templates.${tpl.id}` as UIKeys;
              const translated = t(key);
              const displayName = translated !== key ? translated : tpl.displayName;
              return (
                <div
                  data-template-id={tpl.id}
                  class={`template-card border-2 p-3 rounded-xl bg-white shadow-sm cursor-pointer hover:border-blue-300 transition-all group relative ${i === 0 ? 'border-blue-600 ring-2 ring-blue-100' : 'border-gray-200'}`}
                >
                  <div class="aspect-[4/5] w-full bg-gray-50 rounded-lg p-2 mb-2 border border-gray-100 group-hover:scale-[1.02] transition-transform">
                    <Fragment set:html={tpl.svg} />
                  </div>
                  <div class="text-sm font-bold text-gray-900 text-center group-hover:text-blue-600 truncate tracking-wide mt-1.5">
                    {displayName}
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </div>

      <div class="print:hidden border-t border-gray-200 px-4 sm:px-6 py-3 sm:py-4">
        <button id="btn-publish" class="w-full px-4 sm:px-5 py-2.5 sm:py-3 text-sm font-bold text-white bg-blue-600 hover:bg-blue-700 rounded-xl transition-all">{t('edit.publish')}</button>
      </div>

    </div>

    <div
      id="preview-section"
      class="bg-white p-4 sm:p-6 lg:p-8 rounded-2xl border border-gray-200 shadow-lg xl:min-h-[297mm] print:block print:shadow-none print:border-0 print:p-0 mt-6 lg:mt-0 w-full min-w-0"
      aria-live="polite"
    >
      <header id="preview-header" class="flex justify-between items-start pb-6 border-b border-gray-100">
        <div class="flex-1">
          <h2 id="view-name" class="text-3xl font-bold tracking-tight text-gray-900"></h2>
          <p id="view-label" class="mt-1 text-base text-gray-600 font-medium"></p>
          <div id="view-contact" class="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-xs text-gray-500"></div>
        </div>
        <img id="view-image" class="w-24 h-24 rounded-xl object-cover border border-gray-200 hidden" alt={t('form.photo')} />
      </header>

      <section id="section-summary" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-2 pb-1 border-b border-gray-100">
          {t('section.profile')}
        </h3>
        <p id="view-summary" class="text-sm text-gray-600 leading-relaxed whitespace-pre-line"></p>
      </section>

      <section id="section-experience" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-3 pb-1 border-b border-gray-100">
          {t('section.experience')}
        </h3>
        <div id="view-experience" class="space-y-4"></div>
      </section>

      <section id="section-education" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-3 pb-1 border-b border-gray-100">
          {t('section.education')}
        </h3>
        <div id="view-education" class="space-y-4"></div>
      </section>

      <section id="section-certificates" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-3 pb-1 border-b border-gray-100">
          {t('section.certificates')}
        </h3>
        <div id="view-certificates" class="space-y-4"></div>
      </section>

      <section id="section-languages" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-3 pb-1 border-b border-gray-100">
          {t('section.languages')}
        </h3>
        <div id="view-languages" class="space-y-2"></div>
      </section>

      <section id="section-skills" class="mt-8" style="display:none">
        <h3 class="text-xs font-semibold uppercase tracking-widest text-gray-500 mb-2 pb-1 border-b border-gray-100">
          {t('section.skills')}
        </h3>
        <div id="view-skills-badges" class="flex flex-wrap gap-1.5"></div>
      </section>
    </div>

  </div>

</div>

<Fragment set:html={`<style>${CV_THEMES_CSS}</style>`} />

<script define:vars={{ lang, initialData, currentTranslation: t('edit.currently') }}>
  const STORAGE_KEY = `cv_backup_data_${lang}`;
  const TEMPLATE_KEY = `cv_selected_template_${lang}`;

  function escAttr(s) { return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
  function escText(s) { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }

  const translationsUI = ({
    es: { defaultName: 'Tu Nombre', defaultLabel: 'Tu Profesi\u00f3n u Oficio' },
    it: { defaultName: 'Il Tuo Nome', defaultLabel: 'La Tua Professione' },
    en: { defaultName: 'Your Name', defaultLabel: 'Your Profession' }
  })[lang] || { defaultName: 'Tu Nombre', defaultLabel: 'Tu Profesi\u00f3n' };

  async function loadRemoteResume() {
    try {
      const response = await fetch(`/.netlify/functions/load-resume?lang=${lang}`, {
        method: 'GET',
        credentials: 'include',
        headers: { 'Content-Type': 'application/json' }
      });
      if (!response.ok) return null;
      const result = await response.json();
      if (result.authenticated && result.data) {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(result.data));
        return result.data;
      }
    } catch (err) {
      console.error('Error cargando resume remoto:', err);
    }
    return null;
  }

  let state = (() => {
    try {
      const saved = localStorage.getItem(STORAGE_KEY);
      return saved ? JSON.parse(saved) : initialData;
    } catch (e) {
      return initialData;
    }
  })();

  if (!state.basics) state.basics = { name: '', label: '', email: '', phone: '', summary: '', image: '' };
  if (!state.work) state.work = [];
  if (!state.education) state.education = [];
  if (!state.certificates) state.certificates = [];
  if (!state.languages) state.languages = [];
  if (!state.meta) state.meta = {};
  if (!state.skills) state.skills = [];

  function normalizeMonthValue(value) {
    if (!value) return '';
    return value.slice(0, 7);
  }

  (function normalizeStateDates() {
    state.work.forEach(item => { if (item.startDate) item.startDate = normalizeMonthValue(item.startDate); if (item.endDate) item.endDate = normalizeMonthValue(item.endDate); });
    state.education.forEach(item => { if (item.startDate) item.startDate = normalizeMonthValue(item.startDate); if (item.endDate) item.endDate = normalizeMonthValue(item.endDate); });
    state.certificates.forEach(item => { if (item.date) item.date = normalizeMonthValue(item.date); });
  })();

  function formatDateRange(start, end) {
    const from = normalizeMonthValue(start);
    const to = end ? normalizeMonthValue(end) : currentTranslation;
    if (from && to) return `${from} \u2014 ${to}`;
    return to || from || '';
  }

  const labels = ({
    es: { company: 'Empresa', position: 'Puesto', start: 'Inicio', end: 'Fin', desc: 'Descripci\u00f3n', inst: 'Instituci\u00f3n', area: 'Titulaci\u00f3n', remove: 'Eliminar' },
    it: { company: 'Azienda', position: 'Ruolo', start: 'Inizio', end: 'Fine', desc: 'Descrizione', inst: 'Istituto', area: 'Titolo', remove: 'Elimina' },
    en: { company: 'Company', position: 'Position', start: 'Start Date', end: 'End Date', desc: 'Description', inst: 'Institution', area: 'Area of Study', remove: 'Remove' }
  })[lang];

  function setDeepValue(obj, path, value) {
    const parts = path.split('.');
    let current = obj;
    for (let i = 0; i < parts.length - 1; i++) {
      const key = parts[i];
      if (!(key in current)) current[key] = isNaN(parts[i + 1]) ? {} : [];
      current = current[key];
    }
    current[parts[parts.length - 1]] = value;
  }

  function getDeepValue(obj, path) {
    return path.split('.').reduce((acc, part) => acc && acc[part], obj);
  }

  function saveState() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
    renderPreview();
  }

  function applyTemplate(templateId) {
    document.querySelectorAll('.template-card').forEach(c => {
      if (c.dataset.templateId === templateId.toLowerCase()) {
        c.classList.remove('border-gray-200');
        c.classList.add('border-blue-600', 'ring-2', 'ring-blue-100');
      } else {
        c.classList.remove('border-blue-600', 'ring-2', 'ring-blue-100');
        c.classList.add('border-gray-200');
      }
    });
    state.meta.template = templateId;
    localStorage.setItem(TEMPLATE_KEY, templateId);
    const preview = document.getElementById('preview-section');
    if (preview) preview.setAttribute('data-template', templateId.toLowerCase());
  }

  document.querySelectorAll('.template-card').forEach(card => {
    card.addEventListener('click', () => { applyTemplate(card.dataset.templateId); saveState(); });
  });

  const fileInput = document.getElementById('photo-file');
  const photoStatus = document.getElementById('photo-status');
  const removePhotoBtn = document.getElementById('btn-remove-photo');
  const photoUrlInput = document.getElementById('photo-url');

  if (fileInput) {
    fileInput.addEventListener('change', e => {
      const file = e.target.files[0];
      if (!file) return;
      if (photoUrlInput) photoUrlInput.value = '';
      if (photoStatus) photoStatus.textContent = file.name;
      if (removePhotoBtn) removePhotoBtn.classList.remove('hidden');
      const reader = new FileReader();
      reader.onload = event => { if (event.target.result) { state.basics.image = event.target.result; saveState(); } };
      reader.readAsDataURL(file);
    });
  }

  if (photoUrlInput) {
    photoUrlInput.addEventListener('input', e => {
      const url = e.target.value.trim();
      if (fileInput) fileInput.value = '';
      if (photoStatus) photoStatus.textContent = url ? (lang === 'es' ? 'URL cargada' : lang === 'it' ? 'URL caricata' : 'URL loaded') : (lang === 'es' ? 'Sin archivo' : lang === 'it' ? 'Nessun file' : 'No file');
      if (removePhotoBtn) removePhotoBtn.classList.toggle('hidden', !url);
      state.basics.image = url;
      saveState();
    });
  }

  if (removePhotoBtn) {
    removePhotoBtn.addEventListener('click', () => {
      state.basics.image = '';
      if (fileInput) fileInput.value = '';
      if (photoUrlInput) photoUrlInput.value = '';
      if (photoStatus) photoStatus.textContent = lang === 'es' ? 'Sin archivo' : lang === 'it' ? 'Nessun file' : 'No file';
      removePhotoBtn.classList.add('hidden');
      saveState();
    });
  }

  const tabButtons = Array.from(document.querySelectorAll('.tab-btn'));
  const tabNav = document.getElementById('tab-nav');
  const tabPrev = document.getElementById('tab-prev');
  const tabNext = document.getElementById('tab-next');

  function activateTab(btn) {
    document.querySelectorAll('.tab-btn').forEach(b => {
      b.classList.remove('border-blue-600', 'text-blue-600');
      b.classList.add('border-transparent', 'text-gray-500');
      b.setAttribute('aria-selected', 'false');
    });
    btn.classList.add('border-blue-600', 'text-blue-600');
    btn.setAttribute('aria-selected', 'true');
    document.querySelectorAll('.tab-content').forEach(c => c.classList.add('hidden'));
    const target = document.getElementById('tab-' + btn.dataset.tab);
    if (target) target.classList.remove('hidden');
    btn.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
  }

  tabButtons.forEach(btn => btn.addEventListener('click', () => activateTab(btn)));

  if (tabPrev) {
    tabPrev.addEventListener('click', () => {
      const currentIndex = tabButtons.findIndex(b => b.classList.contains('border-blue-600'));
      const prevIndex = currentIndex <= 0 ? tabButtons.length - 1 : currentIndex - 1;
      activateTab(tabButtons[prevIndex]);
    });
    tabPrev.addEventListener('touchstart', () => tabNav?.scrollBy({ left: -150, behavior: 'smooth' }));
  }

  if (tabNext) {
    tabNext.addEventListener('click', () => {
      const currentIndex = tabButtons.findIndex(b => b.classList.contains('border-blue-600'));
      const nextIndex = currentIndex === -1 || currentIndex === tabButtons.length - 1 ? 0 : currentIndex + 1;
      activateTab(tabButtons[nextIndex]);
    });
    tabNext.addEventListener('touchstart', () => tabNav?.scrollBy({ left: 150, behavior: 'smooth' }));
  }

  function populateFormBasics() {
    document.querySelectorAll('#cv-form input[data-path], #cv-form textarea[data-path]').forEach(input => {
      const path = input.getAttribute('data-path');
      if (path) input.value = getDeepValue(state, path) || '';
    });
    if (state.basics.image) {
      if (state.basics.image.startsWith('data:')) {
        if (photoStatus) photoStatus.textContent = lang === 'es' ? 'Imagen cargada' : lang === 'it' ? 'Immagine caricata' : 'Image loaded';
        if (photoUrlInput) photoUrlInput.value = '';
      } else {
        if (photoUrlInput) photoUrlInput.value = state.basics.image;
        if (photoStatus) photoStatus.textContent = lang === 'es' ? 'URL cargada' : lang === 'it' ? 'URL caricata' : 'URL loaded';
      }
      if (removePhotoBtn) removePhotoBtn.classList.remove('hidden');
    }
  }

  function renderDynamicBlocks() {
    const workContainer = document.getElementById('work-container');
    if (workContainer) { workContainer.innerHTML = ''; state.work.forEach((item, i) => workContainer.appendChild(createWorkBlock(item, i))); }
    const eduContainer = document.getElementById('education-container');
    if (eduContainer) { eduContainer.innerHTML = ''; state.education.forEach((item, i) => eduContainer.appendChild(createEduBlock(item, i))); }
    const certContainer = document.getElementById('certificates-container');
    if (certContainer) { certContainer.innerHTML = ''; state.certificates.forEach((item, i) => certContainer.appendChild(createCertificateBlock(item, i))); }
    const langContainer = document.getElementById('languages-container');
    if (langContainer) { langContainer.innerHTML = ''; state.languages.forEach((item, i) => langContainer.appendChild(createLanguageBlock(item, i))); }
  }

  function createWorkBlock(item, i) {
    const div = document.createElement('div');
    div.className = 'p-4 bg-gray-50 rounded-xl border border-gray-200 relative space-y-3';
    div.innerHTML = `<button type="button" class="absolute top-2 right-2 text-red-500 text-xs font-medium" data-remove="work" data-index="${i}">${labels.remove}</button><div class="grid sm:grid-cols-2 gap-3 pt-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.company}</label><input type="text" data-path="work.${i}.name" value="${escAttr(item.name || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.position}</label><input type="text" data-path="work.${i}.position" value="${escAttr(item.position || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div><div class="grid grid-cols-2 gap-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.start}</label><input type="month" data-path="work.${i}.startDate" value="${escAttr(normalizeMonthValue(item.startDate || ''))}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.end}</label><input type="month" data-path="work.${i}.endDate" value="${escAttr(normalizeMonthValue(item.endDate || ''))}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.desc}</label><textarea data-path="work.${i}.summary" rows="2" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm">${escText(item.summary || '')}</textarea></div>`;
    return div;
  }

  function createEduBlock(item, i) {
    const div = document.createElement('div');
    div.className = 'p-4 bg-gray-50 rounded-xl border border-gray-200 relative space-y-3';
    div.innerHTML = `<button type="button" class="absolute top-2 right-2 text-red-500 text-xs font-medium" data-remove="education" data-index="${i}">${labels.remove}</button><div class="grid sm:grid-cols-2 gap-3 pt-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.inst}</label><input type="text" data-path="education.${i}.institution" value="${escAttr(item.institution || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.area}</label><input type="text" data-path="education.${i}.area" value="${escAttr(item.area || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div><div class="grid grid-cols-2 gap-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.start}</label><input type="month" data-path="education.${i}.startDate" value="${escAttr(normalizeMonthValue(item.startDate || ''))}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${labels.end}</label><input type="month" data-path="education.${i}.endDate" value="${escAttr(normalizeMonthValue(item.endDate || ''))}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div>`;
    return div;
  }

  function createCertificateBlock(item, i) {
    const div = document.createElement('div');
    div.className = 'p-4 bg-gray-50 rounded-xl border border-gray-200 relative space-y-3';
    const certNameLabel = lang === 'es' ? 'Nombre del certificado' : lang === 'it' ? 'Nome certificato' : 'Certificate name';
    const issuerLabel = lang === 'es' ? 'Emisor' : lang === 'it' ? 'Emittente' : 'Issuer';
    const dateLabel = lang === 'es' ? 'Fecha' : lang === 'it' ? 'Data' : 'Date';
    div.innerHTML = `<button type="button" class="absolute top-2 right-2 text-red-500 text-xs font-medium" data-remove="certificates" data-index="${i}">${labels.remove}</button><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${certNameLabel}</label><input type="text" data-path="certificates.${i}.name" value="${escAttr(item.name || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div class="grid sm:grid-cols-2 gap-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${issuerLabel}</label><input type="text" data-path="certificates.${i}.issuer" value="${escAttr(item.issuer || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${dateLabel}</label><input type="month" data-path="certificates.${i}.date" value="${escAttr(normalizeMonthValue(item.date || ''))}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div>`;
    return div;
  }

  function createLanguageBlock(item, i) {
    const div = document.createElement('div');
    div.className = 'p-4 bg-gray-50 rounded-xl border border-gray-200 relative space-y-3';
    const langLabel = lang === 'es' ? 'Idioma' : lang === 'it' ? 'Lingua' : 'Language';
    const levelLabel = lang === 'es' ? 'Nivel' : lang === 'it' ? 'Livello' : 'Fluency';
    div.innerHTML = `<button type="button" class="absolute top-2 right-2 text-red-500 text-xs font-medium" data-remove="languages" data-index="${i}">${labels.remove}</button><div class="grid sm:grid-cols-2 gap-3"><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${langLabel}</label><input type="text" data-path="languages.${i}.language" value="${escAttr(item.language || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div><div><label class="block text-[10px] font-bold text-gray-500 uppercase mb-0.5">${levelLabel}</label><input type="text" data-path="languages.${i}.fluency" value="${escAttr(item.fluency || '')}" class="w-full px-2.5 py-1.5 border border-gray-200 rounded-lg text-sm" /></div></div>`;
    return div;
  }

  function removeByType(type, index) {
    if (type === 'work') state.work.splice(index, 1);
    else if (type === 'education') state.education.splice(index, 1);
    else if (type === 'certificates') state.certificates.splice(index, 1);
    else if (type === 'languages') state.languages.splice(index, 1);
    else if (type === 'skill') state.skills.splice(index, 1);
    else return;
    saveState();
    if (type === 'skill') renderSkillsTags();
    else renderDynamicBlocks();
  }

  [document.getElementById('btn-add-work'), document.getElementById('btn-add-edu'), document.getElementById('btn-add-certificate'), document.getElementById('btn-add-language')].forEach((btn, idx) => {
    if (!btn) return;
    const types = ['work', 'education', 'certificates', 'languages'];
    const empty = [
      { name: '', position: '', startDate: '', endDate: '', summary: '' },
      { institution: '', area: '', startDate: '', endDate: '' },
      { name: '', issuer: '', date: '' },
      { language: '', fluency: '' }
    ];
    btn.addEventListener('click', () => { state[types[idx]].push(empty[idx]); saveState(); renderDynamicBlocks(); });
  });

  const skillsInput = document.getElementById('skills-input');
  const skillsTagContainer = document.getElementById('skills-tag-container');

  function renderSkillsTags() {
    if (!skillsTagContainer) return;
    document.querySelectorAll('.skill-tag-item').forEach(el => el.remove());
    state.skills.forEach((skillObj, idx) => {
      if (!skillObj.name) return;
      const div = document.createElement('div');
      div.className = 'skill-tag-item inline-flex items-center gap-1.5 bg-blue-600 text-white text-xs font-bold px-2.5 py-1 rounded-lg';
      div.innerHTML = `<span>${escText(skillObj.name)}</span><button type="button" class="text-blue-200 hover:text-white font-extrabold ml-0.5 min-w-[44px] min-h-[44px] flex items-center justify-center" data-remove="skill" data-index="${idx}" aria-label="${labels.remove}">&times;</button>`;
      skillsTagContainer.appendChild(div);
    });
    renderPreview();
  }

  if (skillsInput) {
    skillsInput.addEventListener('keydown', e => {
      if (e.key === 'Enter' || e.key === ',') {
        e.preventDefault();
        const val = skillsInput.value.replace(/,/g, '').trim();
        if (val && !state.skills.some(s => s.name.toLowerCase() === val.toLowerCase())) {
          state.skills.push({ name: val });
          saveState();
          renderSkillsTags();
        }
        skillsInput.value = '';
      }
    });
  }

  function toggleSection(id, hasContent) {
    const el = document.getElementById(id);
    if (el) el.style.display = hasContent ? '' : 'none';
  }

  function renderPreview() {
    const nameEl = document.getElementById('view-name');
    const labelEl = document.getElementById('view-label');
    const summaryEl = document.getElementById('view-summary');
    const contactEl = document.getElementById('view-contact');

    if (nameEl) nameEl.textContent = state.basics.name.trim() || translationsUI.defaultName;
    if (labelEl) labelEl.textContent = state.basics.label.trim() || translationsUI.defaultLabel;
    if (summaryEl) summaryEl.textContent = state.basics.summary || '';
    toggleSection('section-summary', !!state.basics.summary);

    if (contactEl) {
      let html = '';
      if (state.basics.email) html += `<span>\u{1F4E7} ${escText(state.basics.email)}</span>`;
      if (state.basics.phone) html += `<span>\u{1F4DE} ${escText(state.basics.phone)}</span>`;
      const website = state.basics.web || state.basics.url || '';
      if (website) html += `<span>\u{1F517} <a href="${escAttr(website)}" target="_blank" rel="noopener noreferrer">${escText(website)}</a></span>`;
      contactEl.innerHTML = html;
    }

    const imgEl = document.getElementById('view-image');
    if (imgEl) {
      if (state.basics.image) { imgEl.src = state.basics.image; imgEl.classList.remove('hidden'); }
      else { imgEl.classList.add('hidden'); }
    }

    const expEl = document.getElementById('view-experience');
    if (expEl) {
      expEl.innerHTML = '';
      state.work.forEach(w => {
        const div = document.createElement('div');
        div.innerHTML = `<div class="flex justify-between font-semibold text-sm"><span class="text-gray-900 font-bold">${escText(w.position || '')}</span><span class="text-gray-500 text-xs">${escText(formatDateRange(w.startDate, w.endDate))}</span></div><div class="text-blue-600 text-xs font-medium">${escText(w.name || '')}</div><p class="text-gray-600 text-xs mt-1 whitespace-pre-line">${escText(w.summary || '')}</p>`;
        expEl.appendChild(div);
      });
      toggleSection('section-experience', state.work.length > 0);
    }

    const eduEl = document.getElementById('view-education');
    if (eduEl) {
      eduEl.innerHTML = '';
      state.education.forEach(e => {
        const div = document.createElement('div');
        div.innerHTML = `<div class="flex justify-between font-semibold text-sm"><span class="text-gray-900 font-bold">${escText(e.area || '')}</span><span class="text-gray-500 text-xs">${escText(formatDateRange(e.startDate, e.endDate))}</span></div><div class="text-gray-500 text-xs font-medium">${escText(e.institution || '')}</div>`;
        eduEl.appendChild(div);
      });
      toggleSection('section-education', state.education.length > 0);
    }

    const certEl = document.getElementById('view-certificates');
    if (certEl) {
      certEl.innerHTML = '';
      state.certificates.forEach(c => {
        const div = document.createElement('div');
        div.innerHTML = `<div class="flex justify-between font-semibold text-sm"><span class="text-gray-900 font-bold">${escText(c.name || '')}</span><span class="text-gray-500 text-xs">${escText(normalizeMonthValue(c.date || ''))}</span></div><div class="text-gray-500 text-xs font-medium">${escText(c.issuer || '')}</div>`;
        certEl.appendChild(div);
      });
      toggleSection('section-certificates', state.certificates.length > 0);
    }

    const langEl = document.getElementById('view-languages');
    if (langEl) {
      langEl.innerHTML = '';
      state.languages.forEach(l => {
        const div = document.createElement('div');
        div.className = 'flex justify-between text-sm text-gray-700';
        div.innerHTML = `<span>${escText(l.language || '')}</span><span class="text-gray-500">${escText(l.fluency || '')}</span>`;
        langEl.appendChild(div);
      });
      toggleSection('section-languages', state.languages.length > 0);
    }

    const badgesEl = document.getElementById('view-skills-badges');
    if (badgesEl) {
      badgesEl.innerHTML = '';
      state.skills.forEach(s => {
        if (!s.name) return;
        const span = document.createElement('span');
        span.className = 'badge-item-live border shadow-xs';
        span.textContent = s.name;
        badgesEl.appendChild(span);
      });
      toggleSection('section-skills', state.skills.length > 0);
    }
  }

  document.querySelectorAll('.btn-print-control').forEach(btn => btn.addEventListener('click', () => window.print()));
  document.querySelectorAll('.btn-download-json-control').forEach(btn => {
    btn.addEventListener('click', () => {
      const a = document.createElement('a');
      a.href = 'data:text/json;charset=utf-8,' + encodeURIComponent(JSON.stringify(state, null, 2));
      a.download = `resume.${lang}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
    });
  });

  document.getElementById('btn-publish')?.addEventListener('click', async () => {
    const btn = document.getElementById('btn-publish');
    if (!btn) return;
    if (document.cookie.indexOf('github_user=') === -1) {
      if (confirm(lang === 'es' ? '\u00bfConectar tu GitHub para publicar tu CV en tu propia web?' : lang === 'it' ? 'Vuoi collegare il tuo GitHub per pubblicare il tuo CV sul tuo sito web?' : 'Do you want to connect your GitHub to publish your CV on your own website?'))
        window.location.href = '/.netlify/functions/auth-login';
      return;
    }
    btn.disabled = true;
    const originalText = btn.textContent;
    btn.textContent = lang === 'es' ? 'Publicando...' : lang === 'it' ? 'Pubblicazione...' : 'Publishing...';
    try {
      const resumeDataByLang = {};
      ['es', 'it', 'en'].forEach(code => {
        try {
          const parsed = JSON.parse(localStorage.getItem('cv_backup_data_' + code) || 'null');
          if (parsed?.basics?.name?.trim()) resumeDataByLang[code] = parsed;
        } catch (e) {}
      });
      if (!resumeDataByLang[lang]) resumeDataByLang[lang] = state;
      const response = await fetch('/.netlify/functions/deploy', {
        method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include',
        body: JSON.stringify({ langs: Object.keys(resumeDataByLang), resumeDataByLang, template: state.meta?.template || localStorage.getItem(TEMPLATE_KEY) || 'minimalist' })
      });
      const data = await response.json();
      alert(response.ok
        ? (lang === 'es' ? '\u00a1Publicado! Tu CV est\u00e1 en: ' + data.webUrl : lang === 'it' ? 'Pubblicato! Il tuo CV \u00e8 su: ' + data.webUrl : 'Published! Your CV is at: ' + data.webUrl)
        : (data.error || (lang === 'es' ? 'Error al publicar.' : lang === 'it' ? 'Errore nella pubblicazione.' : 'Error publishing.')));
    } catch (err) {
      console.error('Error:', err);
      alert(lang === 'es' ? 'Error de conexi\u00f3n.' : lang === 'it' ? 'Errore di connessione.' : 'Connection error.');
    } finally {
      btn.disabled = false;
      btn.textContent = originalText;
    }
  });

  const savedTemplate = state.meta?.template || localStorage.getItem(TEMPLATE_KEY) || 'minimalist';

  const githubBtns = Array.from(document.querySelectorAll('.btn-github-auth-control'));
  const statusTexts = Array.from(document.querySelectorAll('.github-status-text'));

  function updateGitHubAuthStatus() {
    const authed = document.cookie.indexOf('github_user=') !== -1;
    const m = document.cookie.match(/github_user=([^;]+)/);
    const username = m ? m[1] : '';
    githubBtns.forEach(btn => {
      btn.classList.toggle('bg-green-600', authed);
      btn.classList.toggle('hover:bg-green-700', authed);
      btn.classList.toggle('bg-gray-800', !authed);
      btn.classList.toggle('hover:bg-gray-900', !authed);
    });
    statusTexts.forEach(el => { el.textContent = authed ? '\u2713 ' + username : (lang === 'es' ? 'Conectar' : lang === 'it' ? 'Collegare' : 'Connect'); });
  }

  githubBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      if (document.cookie.indexOf('github_user=') !== -1) {
        if (confirm(lang === 'es' ? '\u00bfDesconectar de GitHub?' : lang === 'it' ? 'Disconnettere da GitHub?' : 'Disconnect from GitHub?')) {
          document.cookie = 'github_token=; Max-Age=0; Path=/';
          document.cookie = 'github_user=; Max-Age=0; Path=/';
          updateGitHubAuthStatus();
        }
      } else {
        window.location.href = '/.netlify/functions/auth-login';
      }
    });
  });

  updateGitHubAuthStatus();

  document.getElementById('cv-form')?.addEventListener('input', e => {
    const path = e.target.getAttribute('data-path');
    if (!path) return;
    setDeepValue(state, path, e.target.value);
    saveState();
  });

  document.querySelectorAll('#work-container, #education-container, #certificates-container, #languages-container').forEach(el => {
    if (!el) return;
    el.addEventListener('click', e => {
      const btn = e.target.closest('[data-remove]');
      if (!btn) return;
      e.preventDefault();
      removeByType(btn.getAttribute('data-remove'), parseInt(btn.getAttribute('data-index'), 10));
    });
  });

  document.getElementById('skills-tag-container')?.addEventListener('click', e => {
    const btn = e.target.closest('[data-remove="skill"]');
    if (!btn) return;
    e.preventDefault();
    removeByType('skill', parseInt(btn.getAttribute('data-index'), 10));
  });

  (async () => {
    try {
      const remoteData = await loadRemoteResume();
      if (remoteData) {
        const localImage = state.basics?.image || '';
        state = remoteData;
        if (localImage) state.basics.image = localImage;
      }
      populateFormBasics();
      renderDynamicBlocks();
      renderSkillsTags();
      applyTemplate(savedTemplate);
      renderPreview();
    } catch (e) { console.error('CV Editor init error:', e); }
  })();
</script>

<!-- Cookie Consent Banner -->
<div id="privacy-banner"
  role="dialog"
  aria-label={lang === 'it' ? 'Consenso cookie' : lang === 'en' ? 'Cookie consent' : 'Consentimiento de cookies'}
  aria-modal="false"
  class="fixed bottom-2 sm:bottom-4 left-2 sm:left-4 right-2 md:right-auto md:max-w-md bg-white border border-gray-200 p-3 sm:p-4 rounded-xl shadow-2xl z-50 opacity-0 translate-y-8 pointer-events-none flex flex-col sm:flex-row items-start sm:items-center gap-3 sm:gap-4 transition-all duration-500 ease-out"
>
  <div class="hidden sm:flex flex-shrink-0 w-16 h-16 overflow-hidden items-end self-end -mb-4">
    <img src="https://media1.giphy.com/media/v1.Y2lkPTc5MGI3NjExbG5uaXVwc3ZvY3A0Z2Nvenk0YWZqcWV4amtydHIyOHp4eHNrd2N4ZiZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9cw/CPsnRKLWN0XS9DO4Ej/giphy.gif"
      alt="Cookie consent" class="w-full h-auto object-contain max-h-[110%] origin-bottom scale-110" loading="lazy" />
  </div>
  <div class="flex-1 flex flex-col gap-3">
    <div>
      <h4 class="text-sm font-bold text-gray-900 mb-1">
        {lang === 'it' ? 'Uso dei Cookie' : lang === 'en' ? 'Cookie Usage' : 'Uso de Cookies'}
      </h4>
      <p class="text-xs text-gray-500 leading-relaxed m-0 text-left">
        {lang === 'it'
          ? 'Utilizziamo cookie propri e di terze parti per analizzare le prestazioni del sito e migliorare la tua esperienza. Puoi accettare tutti i cookie o rifiutarli.'
          : lang === 'en'
          ? 'We use first-party and third-party cookies to analyze site performance and improve your experience. You can accept or reject all cookies.'
          : 'Utilizamos cookies propias y de terceros para analizar el rendimiento de la web y mejorar tu experiencia. Puedes aceptar todas las cookies o rechazarlas.'}
        <a href={`/${lang}/cookie-policy`} rel="noopener"
          class="text-blue-600 font-bold underline hover:opacity-80 transition-opacity">
          {lang === 'it' ? 'Cookie Policy' : lang === 'en' ? 'Cookie Policy' : 'Pol\u00edtica de Cookies'}
        </a>
        {lang === 'it' ? ' e ' : lang === 'en' ? ' and ' : ' y '}
        <a href={`/${lang}/privacy-policy`} rel="noopener"
          class="text-blue-600 font-bold underline hover:opacity-80 transition-opacity">
          {lang === 'it' ? 'informativa sulla privacy' : lang === 'en' ? 'privacy policy' : 'pol\u00edtica de privacidad'}
        </a>.
      </p>
    </div>
    <div class="flex items-center gap-2 self-end sm:self-start w-full sm:w-auto">
      <button id="accept-privacy"
        class="flex-1 sm:flex-initial text-center cursor-pointer bg-blue-600 text-white text-xs font-semibold py-2 px-4 rounded-lg shadow-sm hover:brightness-110 transition-all">
        {lang === 'it' ? 'Accetta tutto' : lang === 'en' ? 'Accept all' : 'Aceptar todo'}
      </button>
      <button id="deny-privacy"
        class="flex-1 sm:flex-initial text-center cursor-pointer bg-transparent text-gray-500 border border-gray-200 text-xs font-semibold py-2 px-4 rounded-lg hover:bg-gray-50 transition-all">
        {lang === 'it' ? 'Rifiuta tutto' : lang === 'en' ? 'Reject all' : 'Rechazar todo'}
      </button>
    </div>
  </div>
</div>

<script is:inline>
  (function() {
    var banner = document.getElementById('privacy-banner');
    var btnAccept = document.getElementById('accept-privacy');
    var btnDeny = document.getElementById('deny-privacy');
    if (!banner || !btnAccept || !btnDeny) return;

    var consent = localStorage.getItem('cookie-consent');
    if (!consent) {
      setTimeout(function() {
        banner.classList.remove('opacity-0', 'translate-y-8', 'pointer-events-none');
      }, 300);
    }

    btnAccept.addEventListener('click', function() {
      localStorage.setItem('cookie-consent', 'accepted');
      banner.classList.add('opacity-0', 'translate-y-8', 'pointer-events-none');
      if (!window._gaLoaded) {
        window._gaLoaded = true;
        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        window.gtag = gtag;
        gtag('js', new Date());
        gtag('config', 'G-R3TG90C8J5', { anonymize_ip: true });
        var s = document.createElement('script');
        s.async = true;
        s.src = "https://www.googletagmanager.com/gtag/js?id=G-R3TG90C8J5";
        document.head.appendChild(s);
      }
    });

    btnDeny.addEventListener('click', function() {
      localStorage.setItem('cookie-consent', 'denied');
      banner.classList.add('opacity-0', 'translate-y-8', 'pointer-events-none');
    });
  })();
</script>

<Footer lang={lang} />
</MinimalistLayout>